define [
  'app'
], (
  app
) ->

  app.controller 'ControllerWidgetExerciseBuilderFieldManageOpenResponse', [
    '$scope'
    'apiRequest'
    '$dialog',
    ($scope
      apiRequest
      $dialog) ->
  ]